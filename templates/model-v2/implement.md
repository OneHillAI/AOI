<!-- Implement domain prose. Mark each section with an "item: KEY" HTML comment whose key
     matches a documentation item in entry.yaml. -->

# Implement - <Model Family>

_How do we deploy and run it? Install, hardware, serving, quantization, fine-tuning, and
the safe-deployment controls & Deployment Ceiling._

<!-- item: install -->
## Install & run

<Concrete steps for the common paths - Hugging Face `from_pretrained`, Ollama, vLLM,
llama.cpp. Pin the revision; prefer safetensors. Include copy-pasteable commands.>

```bash
# example
```
